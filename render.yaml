services:
  - type: web
    name: link-remover-bot
    runtime: python
    plan: free
    repo: https://github.com/Artist0455/Link-remover-
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: WEBHOOK_URL
        sync: false
        
